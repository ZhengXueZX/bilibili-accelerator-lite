{
  "name": "bilibili-accelerator-lite",
  "version": "0.1.0",
  "private": true,
  "description": "A conservative Tampermonkey userscript that reorders Bilibili playback CDN URLs.",
  "type": "module",
  "scripts": {
    "check": "node --check src/bilibili-accelerator.user.js && node scripts/verify-version.mjs",
    "release": "node scripts/release.mjs"
  }
}
